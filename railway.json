{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "bash build.sh"
  },
  "deploy": {
    "startCommand":  "python manage.py migrate && gunicorn venturegotchi.wsgi",
    "restartPolicyType": "ON_FAILURE"
  }
}